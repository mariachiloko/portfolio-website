{
  "name": "portfolio-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:content": "node ../scripts/sync-private-content.mjs",
    "dev": "npm run sync:content && vite",
    "build": "npm run sync:content && vite build",
    "preview": "npm run sync:content && vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
